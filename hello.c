#include <stdio.h>
#include <string.h>

// struct student
// {
//     int roll;
//     float cgpa;
//     char name[100];
// };
// void printinfo(struct student s1);
int main()
{
    
    // printf("%d\n",ece.roll);

    // struct student *ptr = &ece;
    // printf("%d\n", ptr->roll);
    // printf("%s\n", ptr->name);
    // printf("%f\n", ptr->cgpa);
    
    // struct student ece[100];
    // ece[0].roll =501;
    // ece[0].cgpa =7.2;
    // strcpy(ece[0].name,"pijush");
    // printf("students name is : %s", ece[0].name);
    // printf("roll no is : %d",ece[0].roll);
    // printf("cgpa:%f",ece[0].cgpa);

    // struct student s1;
    // s1.roll = 1557;
    // s1.cgpa = 9.01;
    // strcpy(s1.name,"pijush kanti adak");
    // printf("student name: %s\n",s1.name);
    // printf("students roll no: %d\n",s1.roll);
    // printf("students cgpa: %f\n",s1.cgpa);

    // char firststr[] = "Apple";
    // char secstr[] = "Bannana";
    // printf("%d",strcmp(firststr,secstr));

    // char firststr[100] = "Hello ";
    // char secondstr[] = "World";
    // strcat(firststr,secondstr);
    // puts(firststr);

    // char name[]= "pijush kanti adak";
    // printf("length is:%d",strlen(name));

    // char *canchange ="hello world";
    // puts(canchange);
    // canchange = "I am Pijush kanti Adak";
    // puts(canchange);

    // char cannotchange[] = "Hello world";
    // puts(cannotchange);
    // cannotchange = "pijush"

    // char firstname[40];
    // printf("enter the firstname:");
    // scanf("%s",firstname);
    // printf("Your Name is:%s",firstname);
    return 0;
}
// void printinfo(struct student s1){
//     printf("student info:\n");
//     printf("student.name: %s\n",s1.name);
//     printf("students.roll no: %d\n",s1.roll);
//     printf("students.cgpa: %f\n",s1.cgpa);
// }