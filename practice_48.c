#include <stdio.h>

int main()
{
    char firstname[40];
    printf("enter the firstname:");
    scanf("%s", firstname);
    printf("Your Firstname is:%s", firstname);
    // char str[100];
    // fgets(str,100,stdin);
    // puts(str);
    // return 0;
}